@namespace Leeax.Web.Components.Modals
@inherits LxComponentBase

<LxModal>
    <LxModalHeader Icon="@Model.Icon" Title="@Model.Title" />
    <LxModalContent>@Model.Text</LxModalContent>
    <LxModalFooter Class="flex-row flex-right">
        @foreach (var curResult in GetResults())
        {
            <LxButton Text="@curResult.ToString()"
                      Size="ComponentSize.Small"
                      Class="ml-2"
                      Clicked="@(() => OnButtonClicked(curResult))" />
        }
    </LxModalFooter>
</LxModal>